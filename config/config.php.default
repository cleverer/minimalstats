<?php
	// minimalstats v0.1
	
	namespace MinimalStats;
	
	class Config {
		
		const DB_HOST = null;
		const DB_USER = null;
		const DB_PASSWORD = null;
		const DB_NAME = null;
		
		const DB_TABLE_PREFIX = 'ms_';

		const SITE_NAME = '';
		
		const LINE_COLOR = 'forestgreen';
		const BACKGROUND_COLOR = '#f7f7f9';
		
		const LINE_POSITION = 'top';
		
		// Probably leave these alone and size the image on your page.
		const WIDTH = 90;
		const HEIGHT = 20;
		const FONT_SIZE = '12px';
	}
